# The one dependency (operator decision A-047): the official MCP Python SDK, used only by agent/mcp_server.py.
# Everything else, including the governed tools and all offline tests, is the Python standard library.
mcp==2.2.0
