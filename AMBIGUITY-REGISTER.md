# Ambiguity Register

Every ambiguity found in the assignment and what was decided about it. Append-only: a reversed row is marked `Reversed` and a new row is appended. Each row has a matching `A-nnn` entry in `PROCESS-LOG.md`.

Where the code depends on a row, the code says so with a comment naming the row ID (search the code for `A-0`).

Short quotes from the private packet are marked with quotation marks. Nothing longer is copied.

**Status rule from 23:31 KST (log D-006):** the operator decides every assumption. A row the agent resolved is `Proposed - awaiting operator` until the operator confirms it; only operator-approved rows are `Confirmed`. Where code depends on a proposed row, the choice is a playbook or config value, so the operator's answer is a data change.

## Register

| ID | Question | Options considered | Recommendation (reading proposed) | Rationale | Reversal trigger | Reversal cost | Status |
|----|----------|--------------------|------------------|-----------|------------------|---------------|--------|
| A-001 | The contract asks for an exact process-log entry format; the operator asked for entries a human can read first. Which wins? | (a) exact six-field format only; (b) one or two plain sentences, then the six fields verbatim in a collapsed `<details>` block | (b) | Both are satisfied: every field is still present in the exact format, and a reader skimming the log gets the decision without expanding anything. | The reviewer's renderer does not expand `<details>`, or the contract owner rejects the wrapper. | Low. A text transform removes the wrapper. | Proposed - awaiting operator |
| A-002 | Which trigger and buyer does the artifact go deep on: the bank with SR 26-2 (Federal Reserve model risk guidance) or the biopharma quality org with FDA PCCP (Predetermined Change Control Plan)? | (a) bank and SR 26-2; (b) biopharma and FDA PCCP; (c) both | Not decided here. The foundation is built trigger-agnostic: triggers, segments and playbooks are data, so either choice is a data change plus one deep brief. | The operator is deciding and the decision arrives separately. Building both would break "pick one, go deep". | The operator's decision arrives. | Low for the foundation, by design. | Confirmed - operator chose (a), see A-027 |
| A-003 | "Do not include mid-market SaaS" and "Remove [AI startups]" versus an ICP sketch row "AI-native SaaS startups". Include or exclude, and how is "AI startup" detected? | (a) keep the row; (b) exclude by segment label only; (c) exclude by segment label plus phrase patterns across naming variations | (c). Excluded by segment and by phrase patterns (AI startup, AI-native, AI-first, GenAI startup, LLM startup, copilot startup, mid-market SaaS and their spelling variants). Bare "AI" never excludes. | The CEO notes are explicit and the operator confirmed. Lists get polluted under many labels, so a label-only check would miss "Gen-AI start-up". Bare "AI" is not a pattern because banks and pharma now describe themselves with it. | The CEO reinstates the segment, or a regulated enterprise is caught by a pattern (a false positive). | Low. Pattern list lives in one file. | Confirmed - operator approved removal |
| A-004 | Is "5k+ employees" a hard floor, and what happens when headcount is unknown? | (a) hard floor, unknown passes; (b) hard floor, unknown is held for a human; (c) soft signal | (b) | It is in the CEO's own ICP sentence, not only the unvetted Slack notes. Unknown is held rather than passed because the motion is precision, not volume. | The CRO says headcount is a soft signal, or enrichment coverage of headcount is poor enough that holding starves the motion. | Low. One rule. | Proposed - awaiting operator |
| A-005 | "Already has agents in production or about to": gate or signal? | (a) hard gate; (b) signal shown in the brief, flagged when unknown | (b). Recorded and flagged, excluded only when enrichment says explicitly "none". | This is rarely knowable before a conversation, so a hard gate would drop most real accounts on missing data rather than on evidence. | The CRO says it is a hard gate. | Low. | Proposed - awaiting operator |
| A-006 | Which accounts count as FS (financial services) for R-17? | (a) only the D-SIB / capital markets segment; (b) segment or industry says FS; unknown counts as FS | (b) | An FS account misclassified as non-FS would escape a required audit. Treating unknown as FS fails safe. | A reliable industry code from the system of record. | Low. | Proposed - awaiting operator |
| A-007 | R-17 says non-FS audit is "recommended, not required". Apply it only to FS or to every segment? | (a) FS only; (b) every segment, same fail-closed rule | (b) | The operator asked for it. One code path means no classification error can switch the audit off, and the FS flag is still recorded on every record. | A throughput or storage constraint on non-FS audit. | Low. | Proposed - awaiting operator |
| A-008 | Where do audit records live? | (a) HubSpot timeline events; (b) a Reign audit store; (c) a local append-only JSONL (JSON Lines) file behind an interface | (c) now, with (a) or (b) as the production sink behind the same interface | No live access in the window. The interface keeps the write-before-complete rule independent of the store. | Access to the real Reign audit store or HubSpot. | Low. New sink class. | Proposed - awaiting operator |
| A-009 | "Do not outbound to the bank until an Executive Assurance Briefing is on the calendar", with a regulatory-trigger exception. How is this enforced? | (a) a note in the brief; (b) a preflight rule: bank-segment outbound is blocked unless a briefing is booked or the trigger type is regulatory | (b) | A note can be ignored; a rule cannot. The exception is part of the rule. | The CEO drops the rule. | Low. | Proposed - awaiting operator |
| A-010 | "40 first meetings" versus "no spray. Precision." How is volume bounded? | (a) no cap; (b) a per-run account cap and kill criteria in the playbook, with a kill switch that blocks every run | (b) | Makes "precision" enforceable and gives the CRO the stop button the Campaign Manager stub describes. | The CRO sets a different mechanism. | Low. | Proposed - awaiting operator |
| A-011 | "Route them without asking": how do risk and engineering both get reached? | (a) ask the buyer; (b) route by title into a risk lane and an engineering lane, same brief, different cover note | (b) | The CEO notes forbid making them declare. Title mapping is deterministic and visible. | Evidence that one lane never engages. | Low. | Proposed - awaiting operator |
| A-012 | HubSpot, Clay and ZoomInfo are the stack, but there is no access in the window. What stands in? | (a) mock inside the pipeline; (b) adapter interfaces with local fixture implementations, one per tool | (b) | Meets the stack where it is and leaves a named seam to wire on day one. | Access is granted. | Low. | Proposed - awaiting operator |
| A-013 | Can fixtures use real company names? | (a) real names; (b) clearly fictional accounts | (b) | Writing invented facts about a real bank or pharma company is exactly the embarrassment the brief warns about. | Real CRM data becomes available. | Low. | Proposed - awaiting operator |
| A-014 | The FDA PCCP guidance page was not found at two guessed URLs (both returned 404). What does the trigger record carry? | (a) keep the guessed URL; (b) carry no URL and mark the source unverified | (b) | Never invent a source. The preflight refuses to build a brief on an unverified source. | The real URL is read from fda.gov. | Low. | Proposed - awaiting operator |
| A-015 | Which model provider, and what happens with no API key? | (a) require a key; (b) Anthropic Messages API over the Python standard library when a key is set, deterministic template otherwise | (b) | Anyone can run the repository from a clean clone with no key, and CI needs no secret. The provider is one setting. | A requirement to use a different provider. | Low. | Proposed - awaiting operator |
| A-016 | How do playbooks version? (stub says unknown) | (a) no versioning; (b) a semantic `version` plus `supersedes`, and every audit record carries playbook id and version | (b) | An auditor needs to know which rules were in force for a given touch. | Campaign Manager already has a versioning scheme. | Low. | Proposed - awaiting operator |
| A-017 | Can one playbook have many triggers? (stub says unknown) | (a) one; (b) many | (a). The stub shape `trigger: {type, id}` is kept. Reuse comes from parameters, not from bundling triggers. | Keeps each audit record tied to one reason. Changing a known field's shape is riskier than keeping it. | Campaign Manager owners confirm multi-trigger playbooks. | Medium. Schema and loader change. | Proposed reversal - see A-030 (awaiting operator) |
| A-018 | What does "briefing" mean as a channel? (stub says unknown) | (a) a calendar booking; (b) a Reign-produced brief pack handed to a named human who may forward it; (c) both | (b). Treated as able to send, because the brief is meant to be forwarded, so it needs a named approver. Calendar booking is out of scope. | Matches "a brief that a CAE (chief audit executive) can forward". Treating it as sending is the stricter reading. | Campaign Manager defines briefing differently. | Low. | Proposed - awaiting operator |
| A-019 | Semiconductor is in the ICP only "if they have a US export-control problem". | (a) include all semiconductor; (b) include only with export-control exposure recorded | (b) | Literal reading of the CEO notes. | CEO widens it. | Low. | Proposed - awaiting operator |
| A-020 | "Do not spray hospitals this month". | (a) exclude hospitals; (b) ignore | (a), as an exclusion. Biopharma quality orgs are a different segment and stay in. | Plain instruction; low cost. | The month rolls over. | Low. | Proposed - awaiting operator |
| A-021 | Rob: "if they do not have a risk committee we are wasting the briefing" (unvetted Slack). | (a) hard gate; (b) flag in the brief | (b) | Unvetted, and it is a signal about briefing value, not eligibility. | Rob or the CEO confirms it as a rule. | Low. | Proposed - awaiting operator |
| A-022 | "Also Canada federal" (unvetted Slack). | (a) add a Canadian federal government segment; (b) do not add | (b), deferred | No owner, no buyer, no trigger. Adding it would be spray. | An owner and a trigger for it. | Low. | Proposed - awaiting operator |
| A-023 | Defense supplier: "Forge-first, Reign later ... lead with the substrate story". | (a) lead with Reign; (b) segment config says lead with Forge, mention assurance | (b) | The CEO's last word on it. | Rob says otherwise. | Low. | Proposed - awaiting operator |
| A-024 | Which pipeline steps are R-17 actions? | (a) sending only; (b) create, update, enrich, score and message, as the rule lists | (b). ICP filter = score; attaching enrichment = enrich; brief and approval request = create; send = message (never executed here). Reading a record is not an R-17 action. | The rule lists these verbs; the operator's research confirmed the scope is broader than sending. | The rule owner says reads count too. | Low. Add an audit call. | Proposed - awaiting operator |
| A-025 | When the preflight cannot tell whether a trigger applies to an account, what happens? | (a) write the brief anyway; (b) hold for a human, with the reason | (b) | A wrong "this rule applies to you" brief sent to a bank is the embarrassment case. | Operator prefers a caveated brief. | Low. | Proposed reversal - see A-028 (awaiting operator) |
| A-026 | Who is the named principal and approver in the runnable demo? | (a) a made-up name; (b) the candidate, who is the operator in this exercise | (b) | The assignment says "You are the operator". Using a real, accountable person avoids inventing one. | A real approver roster. | Low. | Proposed - awaiting operator |
| A-027 | Does "pick one, go deep, do not spray" limit the artifact to one buyer persona, and which trigger goes deep? | (a) one persona only; (b) one artifact type and one deeply implemented trigger, with the engine and playbook covering all three first-motion buyers | (b). SR 26-2 for the bank is implemented deeply. Biopharma quality (FDA PCCP) and the defense supplier are encoded in the playbook with their triggers marked "not implemented yet" and the reason. | Operator decision. The packet says "pick one" about the artifact type (the list of three artifacts) and "a regulatory trigger" in the singular; it does not say to serve one persona. The CEO wants "one playbook we can reuse". | The reviewer reads "go deep" as one persona only. | Low. The other two buyers are data rows, not code. | Confirmed - operator decision (bank and SR 26-2 deep) |
| A-028 | SR 26-2 is Federal Reserve guidance for US banking organisations, most relevant over $30B in assets. What does the brief say for a Canadian D-SIB (domestic systemically important bank)? | (a) hold the account (A-025); (b) write the brief and state "applicability requires confirmation" unless a US Federal Reserve-regulated entity is established, with OSFI E-23 and B-13 as the Canadian context | (b). Supersedes A-025 for this case. | Operator decision. A brief that a CAE (chief audit executive) can forward is more useful than silence, provided it never asserts the rule applies. The Canadian guidelines are the ones that certainly apply. | A confirmed US entity list for the account (then the caveat drops), or legal review says any SR 26-2 mention to a non-US bank is inappropriate. | Low. One preflight branch and one template line. | Proposed - awaiting operator |
| A-029 | What may a brief say about iTmethods and its products? | (a) whatever the model writes; (b) only statements from a sourced claims list, with deterministic checks for forbidden claims | (b). Claims come from `docs/research/product-claims.json`. The output gate rejects any claim of compliance, certification, independent assurance or validation, any briefing duration, and any hint of CMMC (Cybersecurity Maturity Model Certification), FedRAMP (Federal Risk and Authorization Management Program) or CUI capability. | iTmethods' own pages say it makes none of those claims, and they give conflicting briefing durations. One wrong sentence in front of a bank or defense buyer is the embarrassment the packet warns about. | Marketing publishes new approved claims (add rows), or a page changes. | Low. Data file plus word lists. | Proposed - awaiting operator |
| A-030 | The operator wants one first-motion playbook covering all three buyers, each with its own trigger. How does that fit the stub's single `audience` / `trigger` / `channel`? | (a) three separate playbooks; (b) one playbook with a `plays` list, each play keeping the stub's `audience`, `trigger` and `channel` shapes unchanged, and approval, kill criteria and audit shared at the top | (b). Supersedes A-017. Each play still has exactly one trigger, so every audit record is still tied to one reason. | "One playbook we can reuse on the next product launch" (CEO notes) plus the operator's decision. Keeping the stub's field shapes inside each play changes the least known structure. | Campaign Manager owners require one trigger per playbook file. | Medium. Loader and playbook file. | Proposed - awaiting operator |
| A-031 | An account's segment is not in the ICP at all (for example a fintech filed under its own label). Include, exclude or hold? | (a) exclude; (b) hold for a human; (c) include if other rules pass | (b) | The ICP is "regulated enterprise" and a new label may still be one; excluding silently loses it, including it is spray. A hold costs one human look. | The operator lists the full segment set, or says unlisted means out. | Low. One config value (`unlisted_segment`). | Proposed - awaiting operator |

## Reversals

| Original ID | Superseded by | What triggered the reversal | Log entry | Cost actually incurred |
|-------------|---------------|-----------------------------|-----------|------------------------|
| A-017 | A-030 | The operator decided one playbook covers all three buyers, each with its own trigger. | R-002 | None. Recorded before the playbook loader was coded. |
| A-025 | A-028 | The reversal trigger fired as written: the operator prefers a caveated brief ("applicability requires confirmation") to a hold. | R-001 | None. Recorded before the preflight was coded. |

Correction (log R-004): the two reversal rows above say the operator decided A-028 and A-030. That is wrong. Both came from the supervising agent's proposals and await the operator; A-025 and A-017 stand until the operator answers.

## Deferred readings

| From ID | The reading not taken | Why deferred rather than dropped |
|---------|----------------------|----------------------------------|
| A-002 | Going deep on both triggers | One deep brief is the assignment; the other stays a data record. |
| A-022 | Canadian federal government segment | No owner or trigger yet. |
| A-017 | Multi-trigger playbooks | Keeps each touch tied to one reason; revisit if Campaign Manager supports it. |
