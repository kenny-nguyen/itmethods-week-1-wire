# Example output

## Live agent run (first)

[`mcp-20260924T155247Z-d0965b/`](mcp-20260924T155247Z-d0965b/TRANSCRIPT.md) is Claude Code driving the skill and the MCP tools on SR 26-2 for the bank playbook. `evals.score_run` scored it 20/22 (16/16 on the four cases the run exercised), and it includes a blocked attempt on the do-not-contact account.

## Offline test mode (second)

**These are not agent output.** They were drafted by the deterministic offline-test-mode template used for CI, and show the structure, the gate, the audit trail and the review view. Scored agent runs are added when the demo run happens in a Claude Code session.

Generated by `python3 -m agent.run_playbook` on the committed fixtures with no API key. Every company and person is fictional. Absolute paths were replaced with `$WIRE_OUT_DIR`.

| File | What it shows |
|---|---|
| [`20260924T163400Z-c85a71/review.html`](20260924T163400Z-c85a71/review.html) | The one-page review view the run generated: briefs, approval requests, eval score, held, dropped and watched accounts with reasons, the audit trail. Each section links to its source file. Download and open it locally; GitHub shows HTML as source. |
| `20260924T163400Z-c85a71/briefs/` | The Canadian bank (hs-1001): what changed, what is certain (OSFI E-23, B-13), what depends on structure (every line "Confirm"), next step. The US bank (hs-1002): same structure. |
| `20260924T163400Z-c85a71/approvals/` | The requests routed to the named account owner: `status: pending`, `send: false`. |
| `20260924T163400Z-c85a71/audit.jsonl` | Every R-17 record this run wrote, in order. |
| `20260924T163400Z-c85a71/summary.json` | Every account's outcome and reason, the watch list, each playbook's status and metrics. |
| `20260924T163400Z-c85a71/scores.json` | The run scored by `python3 -m evals.score_run` (22/22), labelled offline test mode. |

The Markdown and JSON files are the source of truth; the HTML view is generated from them.

Each brief is also written as two files: `<account>.brief-forwardable.md` (public sources only, safe to forward: no system ids, recipients or lanes) and `<account>.owner-notes.md` (everything internal for the account owner).
